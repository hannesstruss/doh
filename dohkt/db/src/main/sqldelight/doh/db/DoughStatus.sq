CREATE TABLE DoughStatus (
    id TEXT NOT NULL PRIMARY KEY,
    doughIndex INTEGER NOT NULL,
    recordedAt TEXT NOT NULL,
    growth REAL NOT NULL
);

insert:
INSERT INTO DoughStatus
VALUES (?, ?, ?, ?);

getAll:
SELECT * FROM DoughStatus LIMIT ?, ?;
